Примеры команд:
python worker_poller.py --recruiters 1,2,3 (обслуживает только этих троих)
python worker_poller.py --recruiters 4,5,6 (обслуживает других)
python worker_poller.py (без аргументов — по умолчанию берет всех из БД)

